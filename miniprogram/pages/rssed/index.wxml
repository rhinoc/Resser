<view class="container">
  <l-button width="750" special="true" size="long" bind:lintap="onAdd">
    <view class="box">
      <l-icon size="40" name="add" color="#3963bc" />
    </view>
  </l-button>
</view>

<l-slide-view wx:for="{{rss_list}}" wx:key="{{index}}" key="{{index}}" height="140" width="750" slide-width="400" auto-close="true">
  <view slot="left" class="left">
    <view class='left-con'>
      <text class='left-title'>{{item.title}}</text>
      <text class='left-time'>{{item.description}}</text>
    </view>
    <view>
      <image class='favicon' src="{{item.favicon}}"></image>
    </view>
  </view>
  <view slot="right" class="right">
    <view class="share" bindtap='onShare' data-id='{{index}}'>分享</view>
    <view class="edit" bindtap='onEdit' data-id='{{index}}'>修改</view>
    <view class="del" bindtap='onDel' data-id='{{index}}'>取消订阅</view>
  </view>
</l-slide-view>


<l-popup show="{{show}}" contentAlign="bottom" bind:lintap="onAdd">
  <view class='popup'>
    <l-input label="名称" placeholder="例：爱范儿" bind:linchange="onName" />
    <l-input label="订阅地址" placeholder="例：https://www.ifanr.com/feed" bind:linchange="onRss" />
    <l-input label="标签" placeholder="例：科技" bind:linchange="onTag" />
    <view class = "buttons">
    <l-button type="default" bind:lintap='onSubmit'>保存</l-button>
    <l-button type="default" plain bind:lintap='onCancel'>取消</l-button>
    </view>
    </view>
</l-popup>
